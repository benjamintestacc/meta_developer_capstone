import React from "react";
import Logo from "../../../img/icons/little-lemon-logo.svg";

const Header = () => {
  return (
    <header>
      <img src={Logo} alt="Logo" />
    </header>
  );
};

export default Header;
